This is the Print plug-in for the Gimp, version 3.1.5 (development).

To build and install:

./configure
make
make install

This is a development release, and as such, not all printers or
combinations of options will work.  Please check our web site at
http://gimp-print.sourceforge.net for details about what is and is not
supported.

If configure is not there, run prior to configure:
aclocal;autoconf;automake

To build and install the GhostScript driver, please see the file
Ghost/README.
